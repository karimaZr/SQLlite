<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SqlLite" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SqlLite" parent="Base.Theme.SqlLite" />

    <style name="Theme.SqlLite.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SqlLite.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.SqlLite.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>